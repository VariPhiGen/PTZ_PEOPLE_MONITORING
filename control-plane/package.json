{
  "name": "acas-control-plane",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev":       "wrangler dev",
    "deploy":    "wrangler deploy",
    "typecheck": "tsc --noEmit",
    "db:init":   "wrangler d1 execute ACAS_DB --local --file=schema.sql"
  },
  "dependencies": {
    "hono": "^4.12.8"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240909.0",
    "typescript": "^5",
    "wrangler": "^3"
  }
}
