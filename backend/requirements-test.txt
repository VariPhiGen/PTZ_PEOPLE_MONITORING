# Test dependencies — install ALONGSIDE requirements.txt
# pip install -r requirements-test.txt
pytest>=8.2
pytest-asyncio>=0.23
pytest-cov>=5.0
pytest-timeout>=2.3
httpx>=0.27
fakeredis[aioredis]>=2.23
factory_boy>=3.3
Faker>=25.0
asgi-lifespan>=2.0
# CPU-only FAISS for tests (replaces faiss-gpu-cu12 on GPU-less CI runners)
# Do NOT install both faiss-cpu and faiss-gpu-cu12 in the same env.
faiss-cpu>=1.8
